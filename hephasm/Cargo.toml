[package]
name = "hephasm"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { path = "../shared" }
parseid = { path = "../parseid" }
lexariel = { path = "../lexariel" }
thiserror = "1.0"

[dev-dependencies]
